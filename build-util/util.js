let bundler = function(file){
    replacer(file);
    mover(file);
}

let replacer = function(){
    // take html input

    // go through each line

    // if line contains [-

    // replace with value
}

let mover = function(){

}

module.exports = bundler;